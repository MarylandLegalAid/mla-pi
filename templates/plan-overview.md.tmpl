# {{PROJECT}} — Implementation Plan

Written by `/skill:plan` on {{DATE}}. Executed by `/skill:build`.

## Goal

{{GOAL}}

## Architecture

{{ARCHITECTURE}}

## Decisions ledger

Every decision below was made by the user during the interview. **Implementers do
not revisit these.** If one turns out to be wrong, stop and report — do not
silently choose differently.

| # | Decision | Resolution | Why |
|---|---|---|---|

## Conventions to follow

Quoted from `research/codebase.md`, with citations. New code matches these.

- {{CONVENTION}} — `{{FILE}}:{{LINE}}`

## Tooling

What this task needed, resolved on this machine. Platform: {{OS_ID}} {{OS_VERSION}}
({{FAMILY}} family, {{ARCH}}), package manager {{MANAGER}}.

| Tool | Status | Scope | Used for |
|---|---|---|---|
| {{ID}} | installed / already-present / declined / manual | global\|project | {{WHY}} |

Outstanding manual steps (sudo installs, interactive logins) not yet done:
- [ ] {{COMMAND}} — {{WHY}}

## Environment

- Local dev: `{{DEV_COMMAND}}`
- Tests: `{{TEST_COMMAND}}`
- Typecheck: `{{TYPECHECK_COMMAND}}`
- Lint: `{{LINT_COMMAND}}`
- Production path: {{PROD_PATH}} — **never executed by build**

## Work packages

| id | depends_on | owns | summary |
|---|---|---|---|

## Waves

- **Wave 1:** {{IDS}}
- **Wave 2:** {{IDS}}

## Manual steps

Packages marked `manual: true`, in the order they will be reached.

## Out of scope

What this plan deliberately does not do, so build does not drift into it.
